{{ form_start(form) }}
    <div>{{ form_errors(form) }}</div>
    <div>{{ form_label(form.title, 'Ingresa el titulo:') }}</div>
    <div>{{ form_widget(form.title, {'attr': {'placeholder': 'Title', 'color': 'grey'}}) }}</div>
    <div>{{ form_label(form.text, 'Ingresa el texto:') }}</div>
    <div>{{ form_widget(form.text, {'attr': {'placeholder': 'Text', 'color': 'grey'}}) }}</div>
    <div>
        <button type="submit">Guardar</button>
    </div>
{{ form_end(form) }}
